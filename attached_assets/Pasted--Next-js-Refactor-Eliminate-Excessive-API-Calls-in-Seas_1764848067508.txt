# Next.js Refactor: Eliminate Excessive API Calls in SeasonHub Component

**Task:** Refactor the `sAlias.txt` component to implement **Variant A: Complete removal of useEffect for data fetching**. Use ALL data from `getStaticProps` props directly. No state duplication, no client-side re-fetching of data already loaded in SSG.

## EXACT CHANGES REQUIRED:

### 1. REMOVE these lines completely:
❌ const [matchdaysForRound, setMatchdaysForRound] = useState<MatchdayValues>(selectedRoundMatchdays)
❌ const [displayMatches, setDisplayMatches] = useState<MatchValues>(...)
❌ THE ENTIRE useEffect block with fetchData() - DELETE COMPLETELY
❌ Any setMatchdaysForRound() or setDisplayMatches() calls


### 2. REPLACE with derived values using useMemo:
const pageMode = mdAlias ? 'MATCHDAY' : rAlias ? 'ROUND' : 'SEASON'
const currentRound = rAlias ? allRounds.find(r => r.alias === rAlias) : null

const matchdaysForRound = useMemo(() => {
return selectedRoundMatchdays
.filter((md: MatchdayValues) => md.published)
.sort((a: MatchdayValues, b: MatchdayValues) => {
if (a.startDate && b.startDate) {
return new Date(a.startDate).getTime() - new Date(b.startDate).getTime()
}
return a.alias.localeCompare(b.alias)
})
}, [selectedRoundMatchdays])

const displayMatches = useMemo((): MatchValues => {
switch (pageMode) {
case 'MATCHDAY': return selectedMatchdayMatches
case 'ROUND': return selectedRoundMatches
default: return selectedSeasonMatches
}
}, [pageMode, selectedMatchdayMatches, selectedRoundMatches, selectedSeasonMatches])



### 3. UPDATE all references:
- Replace `matchdaysForRound.length === 1` → use `matchdaysForRound.length === 1`
- Replace `displayMatches.length === 0` → use `displayMatches.length === 0`
- MatchList: `matches={displayMatches}` and `matchdays={pageMode === 'ROUND' ? matchdaysForRound : []}`
- Empty state: use `pageMode` for conditional messaging
- Standings: `pageMode === 'MATCHDAY' && selectedMatchday?.createStandings`

### 4. Handler functions STAY THE SAME (router.push only):
// Keep existing handleSeasonChange, handleMatchdayChange exactly as-is
// They trigger new getStaticProps via navigation


### 5. getStaticProps & getStaticPaths: NO CHANGES REQUIRED
Keep exactly as currently implemented.

## RULES:
- ✅ Use props DIRECTLY: `selectedSeasonMatches`, `selectedRoundMatches`, `selectedMatchdayMatches`, `selectedRoundMatchdays`
- ✅ Keep ALL existing UI/JSX structure, styling, breadcrumbs, dropdowns unchanged
- ✅ Keep ALL existing handler functions unchanged  
- ✅ Preserve TypeScript types exactly
- ✅ ONLY remove the problematic useEffect + related state
- ❌ NO new API calls in component
- ❌ NO SWR/React Query (pure props-based)
- ❌ NO changes to getStaticProps logic

## GOAL:
Reduce from 50+ API calls to 5-9 calls per page (handled by getStaticProps only).

**Generate the complete refactored `SeasonHub` component with imports intact.**
